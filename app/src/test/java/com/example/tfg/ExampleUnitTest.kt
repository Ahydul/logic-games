package com.example.tfg

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    /*
    @Test
    fun addition_isCorrect() {
        val regions = Hakyuu.randomRegionSizes(maxNumberOfRegions = 5, maxRegionSize = 4, numPositions = 36)

        assertEquals(36, regions.foldIndexed(0) { index, acc, value -> acc + (index+1)*value })
    }



    @Test
    fun pity() {
        val numberOfRegions = 15
        val regions = Regions(6,6, Random(1238734)).divideRegionsOptionB(numberOfRegions)



        assert(regions.size >= numberOfRegions)
    }
    */
}